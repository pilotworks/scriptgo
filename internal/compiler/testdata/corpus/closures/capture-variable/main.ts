const multiplier = 3;
const mult = (x: number): number => x * multiplier;
console.log(mult(10));

const greeting = "Hello";
const greet = (name: string): string => `${greeting}, ${name}!`;
console.log(greet("ScriptGo"));
