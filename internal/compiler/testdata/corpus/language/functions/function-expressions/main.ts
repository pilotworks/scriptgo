const multiply = function(a: number, b: number): number {
    return a * b;
};

const greet = function(name: string): string {
    return "Hello, " + name + "!";
};

console.log(multiply(6, 7));
console.log(greet("ScriptGo"));
