# Managed Values

This directory is reserved for value representation and ownership policies
shared by native runtime families. Add a focused subdirectory only when the
corresponding ABI and native implementation are ready.
