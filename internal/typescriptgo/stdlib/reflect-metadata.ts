// ScriptGo Standard Library: reflect-metadata compatibility module.
// Metadata reflection is exposed through the global Reflect declarations and
// lowered natively; this module exists so side-effect imports resolve without
// a project-local node_modules dependency.
