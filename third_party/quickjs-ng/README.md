# QuickJS-ng

ScriptGo vendors the QuickJS-ng amalgam used by the opt-in Dynamic runtime.

- Version: `v0.10.1`
- Revision: see `REVISION`
- License: MIT, see `LICENSE`

The engine is compiled into generated executables only when a Dynamic IR call
is present. Static artifacts do not link this source.
